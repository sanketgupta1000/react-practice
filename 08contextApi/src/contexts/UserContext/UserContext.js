import React from 'react'

// will create context for user
// think of this like a global space for user related things
// ideally, many contexts can be there for the same app

const UserContext = React.createContext()

export default UserContext