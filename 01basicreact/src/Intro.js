// when we create project with create-react-app, it doesn't have so many constraints
// like components doen't need to have jsx extension
// but their names need to start with uppercase

function Intro()
{
    return (
        <h2>Hello, my name is Sanket Gupta</h2>
    )
}

export default Intro